# Copy this file to .env and fill in your real API keys.
# The .env file is git-ignored. Never commit real keys.

# OpenAI (used for technical feature extraction + knowledge-based analysis)
OPENAI_API_KEY=
# OpenAI model ID as exposed by the OpenAI API (not the ChatGPT display name)
OPENAI_MODEL=