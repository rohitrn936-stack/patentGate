# Convenience installer for the whole repo (llm/ + every agent package).
# Prefer:  pip install -e ".[dev]"
#
# Runtime deps are declared in pyproject.toml. This file mirrors the common set
# so `pip install -r requirements.txt` still works.
-e .[agents]
pytest>=8.0
pytest-asyncio>=0.23
